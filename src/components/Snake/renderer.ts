import { Player } from "./player";


// player does contain all the import information about board and its location
export function renderer(context: CanvasRenderingContext2D, player: Player) {
    return; 
}
